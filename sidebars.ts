// docs plugin is disabled (docs: false in docusaurus.config.ts).
// This file is kept as a placeholder in case docs are re-enabled.
import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

const sidebars: SidebarsConfig = {};

export default sidebars;
